import { Box } from '@mui/material';
import styled from 'styled-components';



export const CustomPaper = styled(Box)`
  && {
    position: fixed;
    bottom: 0px;
    left: 0px;
    right: 0px;
  }
`